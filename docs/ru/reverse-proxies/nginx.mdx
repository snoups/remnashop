---
title: "Nginx"
---

В этом руководстве мы будем использовать Nginx в качестве обратного прокси для доступа к боту Remnashop. Мы направим доменное имя на наш сервер, выпустим SSL-сертификат и настроим Nginx.

<Warning>
    Для продолжения вам необходимо иметь зарегистрированное доменное имя.
</Warning>

---

## Направьте домен на ваш сервер

Узнайте IP-адрес вашего сервера. Рекомендуется использовать статический IPv4-адрес.

Теперь вам нужно направить доменное имя на этот IP-адрес.

Например: `my-super-panel.com` -> `193.122.122.122`

Это можно сделать двумя способами:

1. Через DNS-провайдера (например, Cloudflare, Google Cloud и т.д.)
2. Через регистратора домена (например, Namecheap и т.д.)

---

## DNS-провайдер

Если вы используете Cloudflare, вам нужно добавить запись A/AAAA (для IPv4 и IPv6 соответственно) в ваши DNS-записи.

Войдите в ваш аккаунт Cloudflare [здесь](https://dash.cloudflare.com/login) и выберите нужный домен.

В левой части страницы нажмите на `DNS`, затем на `Records`. Нажмите `Create record`.

Установите `Type` в значение `A`, а `Name` в значение `@`.

<Info>
    Если вы хотите использовать поддомены, введите имя поддомена (например, `bot`) в поле `Name`.
</Info>

Введите IP-адрес вашего сервера в поле `IPv4 address` и нажмите `Save`.

Теперь нужно подождать некоторое время, пока DNS-записи обновятся.

<Info>
    В панели управления Cloudflare есть важное различие между жёлтым облаком (домен проксируется через Cloudflare) и серым облаком (домен не проксируется через Cloudflare).
    Если Cloudflare хорошо работает в вашем регионе, рекомендуется проксировать домен через Cloudflare (жёлтое облако).
</Info>

<Frame>
    <img src="/images/cloudflare-dns.webp" />
</Frame>

У некоторых DNS-провайдеров интерфейс отличается, но общий процесс одинаков.

---

## Настройка Nginx

Вы можете установить Nginx на том же сервере, что и панель Remnawave, или на отдельном сервере. Выберите подходящий вариант ниже.

<Columns cols={2}>
    <Card
        title="📄️ На одном сервере"
        href="/docs/ru/reverse-proxies/nginx/bundled"
        arrow="true"
        cta="Перейти"
    >
    Установка на одном сервере означает, что Nginx будет настроен на том же сервере, что и панель Remnawave.
    </Card>
    <Card
        title="📄️ На отдельном сервере"
        href="/docs/ru/reverse-proxies/nginx/separate-server"
        arrow="true"
        cta="Перейти"
    >
    Установка на отдельном сервере означает, что Nginx будет установлен на другом сервере, отличном от сервера с панелью Remnawave и остальными компонентами.
    </Card>
</Columns>